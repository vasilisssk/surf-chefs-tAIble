## Использованные инструменты: Android Studio | Codex | Qoder (выдали про план бесплатно как новому пользователю)

## Промпты:
1. [src](app/src/) .gitignore [build.gradle.kts](app/build.gradle.kts) [proguard-rules.pro](app/proguard-rules.pro)  [conclusion](01-analysis/conclusion/) [er-model-mermaid-final.md](01-analysis/er-model/er-model-mermaid-final.md) [AndroidManifest.xml](app/src/main/AndroidManifest.xml) [build.gradle.kts](build.gradle.kts) [settings.gradle.kts](settings.gradle.kts) .gitignore [02-development](02-development/) можешь проанализировать ТЗ и описать доменные сущности
2. по сути бэкенда нет, его нужно реализовать. давай для начала продумаем архитектуру и инструменты. я работал с Java и Spring Boot, так что я смогу валидировать написанный код. по архитектуре, вроде, spring boot app использует model-view-controller. так же нужно будет подлючить docker. что скажешь по плану?
3. отличный стек, давай теперь перейдем к frontend, здесь я думаю все просто: kotlin + mvvm. насчет backend обязательно добавить логирование для сетевых вызовов, это очень важно для отладки
4. для DI давай использовать Koin - я с ним работал. насчет верстки здесь [layout](app/src/main/res/layout/) лежат уже готовые фрагметны и активность, над ними нужно будет поработать и добавить парочку новых активностей и фрагментов, так что xml оставляем. можешь в папке [backend](02-development/backend/) написать md-файл check list с ограничениями из тз для backend части приложения, то же самое в папке @02-development/frontend/ но уже для frontend
5. замечательно, забыл добавить, что не нужно писать классы на 1000 строк кода, каждый entity - отдельный класс, это касается и frontend (viewmodel, entity, dto ) и backend (сервисы, контроллеры), обязательно добавить это в оба checklist
6. /init
7. можешь приступать к реализации backend, обязательно отметь в чек-листе, что получилось реализовать [backend](C:/Users/Vasilisssk/AndroidStudioProjects/ChefsTable/02-development/backend/) , разбей все по соответсвующим директориям
# Закончились бесплатные промпты в Codex'е, так что перешел в Qoder
8. [backend](02-development/backend) у меня есть backend как отдельный проект в AndroidStudio, там есть docker-compose.yml и Dockerfile, что мне нужно сделать, чтобы пользоваться этим backend?
9. <код ошибки> ошибка при валидации схемы Hibernate, можешь исправить, что необходимо. И как мне заново пересобрать Image и контейнеры если нужно
10. работает отлично. есть пара вопросов: 1. при запуске контейнеров проходят ли тесты из папки test 2. как мне открыть порт на котором находится бэк, потому что при открытии ссылки из докера http://localhost:8080/ ошибка 404, а при открытии http://localhost:8080/v1 ошибка 403, то же самое при открытии http://localhost:8080/v1/swagger-ui.html
11. в папке [frontend](02-development/frontend)  лежит чеклист с тем, что нужно реализовать и как. в папке [conclusion](01-analysis/conclusion) лежат логика и экрана приложения. в папке [backend](02-development/backend/src/main/java/com/chefstable/backend) лежит backend часть приложения, поднятая в докере. мне нужно чтобы ты реализовал клиентскую часть приложения, обязательно пометь, что ты выполнил в чек листе 
12. [main](app/src/main) в приложении есть несколько багов в активностях регистрации и входа: поля ввода сливаются с задним фоном - все белое, цвет текста тоже сливается, ни вход, ни регистрация не работают - Ошибка сети: <-- HTTP FAILED: java.net.SocketTimeoutException: failed to connect to (port 8080) from (port 48960) after 30000ms
13. есть парочка багов: в профиле нет поля фамилии, в бд оно пустое - нужно добавить поле и при нажатии на кнопку обновить данные, чтобы фамилия шла в бд. очень важно то, что кнопка назад в подтверждении бронирования и в деталях заказа не работает - пользователь постоянно остается в этих активностях, нельзя взаимодействовать  с полем для ввода шефа в расписании
14. я имел в виду кнопку назад сделать не на фрагменте, а на активности же  вроде кнопка есть, сверху еще около которой отображается название фрагмента, она не работала, убири новую кнопку и добавь функционал кнопке сверху активности или фрагмента. с шефом в расписании все так же нельзя взаимодействовать и у фрагментов очень большой отсутуп сверху, это выглядит некрасиво. ближайшее бронирование в главной показывает наоборот дальнейшее. и также нужно исправить стиль ошибок в активности входа - сейчас там вылезает snackbar, а нужно сделать как в активности регистрации, чтобы был одинаковый дизайн и поведение
# Закончились кредиты в Qoder, перешёл на бесплатную модель
15. можешь довести дела прошлого запроса до конца?
16. что нужно доделать: сделать маску для номера +7, потом сделать поле ввода для шефов примерно как в активности регистрации - сейчас там есть проблема с отсупами, текст вплотную к краям поля
17. маска работает отлично, только с ней выскакивает ошибка, что неверный номер, значит проблема скорее всего либо в валидации в самом фрагменте, либо в его viewmodel , причем в бд нужно чтоб номер шел в одном формате: +79999999999 - пример, и я поправил максимальное количество символов в поле ввода для телефона на 18
18. при регистрации выдает Произошла ошибка. Попробуйте позже, но теперь валидация телефона проходит
19. в этой папке лежит бэк [backend](02-development/backend). Сейчас при выходе из приложения, сессия не сохраняется, я так понимаю это связано с пунктом  ## Security - [ ] Хранить токены безопасно и проверять их валидность. Нужно реализовать это, в чек листе поментить, если получилось, если нет, так и скажи что мешает реализации
20. нужно пересобрать контейнеры в докере, но ты изменил базу, так что ее нужно очистить насколько я понимаю, как это сделать? и можешь добавить в данные класс, в котором будет всего одно место для тестов
21. Это не работает. Вот логи: --> GET http://10.0.2.2:8080/v1/profile Authorization: Bearer eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJjbGllbnRfNDViZTA2NWUzZTEwNDM2NDg1NmZmZGQxZjM2MWRmNmUiLCJpYXQiOjE3ODMyNjc2MTksImV4cCI6MTc4MzM1NDAxOX0.wbjKclXQ1LfSM3pKc7F_dXJgoLPRBi9vAm3VPOQApMyoWu6TaP_S21kitPb6a1KvIHVBjAOA34y9RvPUGGhsTA  --> END GET <-- 200 http://10.0.2.2:8080/v1/profile (23ms)
22. в этой папке лежит бэк [backend](02-development/backend), из ## Business rules from TЗ нужно реализовать - [ ] Повторная запись на отменённый студией слот должна быть запрещена. Напиши, получится ли это сделать с текущей бд, если да, то приступай к работе, елси нет, то напиши, что нужно доделать 
23. ты пометил в чек-листе, что выполнил? обязательно сделай это
24. Ошибка осталась, вот логи: {"action":"cancel"} --> END PUT (19-byte body) <-- 200 http://10.0.2.2:8080/v1/bookings/booking_65cb8832f9d24a21abb7ba9fb3488ba3 (55ms) бронь отменяется, но на главной я снова могу успешно забронировать уже отмененный класс <-- 200 http://10.0.2.2:8080/v1/classes (23ms)
25. я заметил, что на главной в разделе ближайшие бронирования нельзя отменить бронь, кнопка отмены не работает, не появляется диалог с отменой. я думаю, что лучше вместо отмены добавить кнопку для перехода в мои бронирования, чтобы кнопка отмены отображалась только там
26. кнопка Мои бронирования появилась на главной, но теперь это кнопка добавилась и в мои бронирования. скорее всего для адаптера используется единая карточка, нужно добавить новую, либо поработать с логикой кнопок
27. теперь при нажатии мои бронирования на главной, всегда открывается мои бронирования если нажимать на главную
28. при подтверждении бронирования нельзя выбрать пакет проката, постоянно отображается Basic (+300 руб) 1. в rantal_package в бд добавить вариант проката "Со своим" 2. добавить описание в подтверждении бронирования, что из себя представляет вариант проката - в бд уже есть описание, просто его вставить туда, 3. сделать так, чтобы можно было выбирать варианты проката
29. [main](app/src/main) здесь у меня фронт мобильного приложения, во фрагментах главного экрана, расписания, моих броней карточки адаптера залезают под bottom navigation view - нужно это исправить